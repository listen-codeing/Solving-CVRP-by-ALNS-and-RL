# 环境配置与常见问题解决指南

## 问题1：ImportError - cannot import name 'move_graph_to_device' from 'train'

### 错误信息
```
ImportError: cannot import name 'move_graph_to_device' from 'train'
(C:\...\site-packages\train\__init__.py)
```

### 原因分析
这是一个**模块命名冲突**问题：
1. 代码中 `test.py` 原本写的是 `from train import move_graph_to_device`
2. 但项目文件名是 `train_reinforce.py`，不是 `train.py`
3. 如果你的Python环境中安装了名为 `train` 的第三方包，Python会优先导入第三方包
4. 导致找不到 `move_graph_to_device` 函数

### 解决方案

**方案1：使用修复后的代码（推荐）**
```bash
# 拉取最新代码（已修复）
git pull origin claude/code-review-analysis-011CUt4q3fpiSZwfsxCrT7JB
```

**方案2：手动修改（如果没有用Git）**

打开 `test.py`，找到第12行：
```python
# 修改前
from train import move_graph_to_device

# 修改后
from train_reinforce import move_graph_to_device
```

### 预防措施
避免Python文件名与常见的第三方包重名，例如：
- ❌ `train.py` （与 `train` 包冲突）
- ❌ `test.py` （可能与 `pytest` 等冲突）
- ❌ `model.py` （常见冲突）
- ✅ `train_reinforce.py` （推荐）
- ✅ `vrp_test.py` （推荐）

---

## 问题2：PyTorch Geometric安装失败

### 错误信息
```
ERROR: Could not find a version that satisfies the requirement torch-geometric
```

### 解决方案

**步骤1：确认PyTorch版本**
```bash
python -c "import torch; print(torch.__version__)"
# 例如输出: 2.1.0+cu118
```

**步骤2：根据PyTorch版本安装PyG**

访问 [PyTorch Geometric 官网](https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html)

或使用以下命令（以PyTorch 2.1.0为例）：
```bash
# CPU版本
pip install torch-geometric

# CUDA版本（需要先安装对应的PyTorch CUDA版本）
pip install torch-geometric
```

**步骤3：验证安装**
```bash
python -c "import torch_geometric; print(torch_geometric.__version__)"
```

---

## 问题3：运行时CUDA错误

### 错误信息
```
RuntimeError: CUDA out of memory
```

### 解决方案

**方案1：减小批大小**

修改 `train_reinforce.py` 第123行：
```python
# 修改前
batches = data_generator(num_customers, vehicle_capacity, instances=120, batch_size=6)

# 修改后（减小batch_size）
batches = data_generator(num_customers, vehicle_capacity, instances=120, batch_size=2)
```

**方案2：使用CPU训练**

修改 `train_reinforce.py` 第70行：
```python
# 修改前
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')

# 修改后（强制使用CPU）
device = torch.device('cpu')
```

**方案3：减小问题规模**

修改 `train_reinforce.py` 第98-99行：
```python
# 修改前
num_customers = 100
vehicle_capacity = 50

# 修改后
num_customers = 50   # 减小客户数量
vehicle_capacity = 30
```

---

## 问题4：matplotlib中文显示问题

### 错误现象
图表中的中文显示为方框 `□□□`

### 解决方案

**Windows系统**：
```python
# 在test.py顶部添加
import matplotlib.pyplot as plt
plt.rcParams['font.sans-serif'] = ['SimHei']  # 使用黑体
plt.rcParams['axes.unicode_minus'] = False    # 解决负号显示问题
```

**Linux系统**：
```bash
# 安装中文字体
sudo apt-get install fonts-wqy-zenhei

# 清除matplotlib缓存
rm -rf ~/.cache/matplotlib
```

**macOS系统**：
```python
plt.rcParams['font.sans-serif'] = ['Arial Unicode MS']
```

---

## 完整的环境配置流程

### 步骤1：创建虚拟环境（推荐）

**使用venv**：
```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows
venv\Scripts\activate
# Linux/macOS
source venv/bin/activate
```

**使用conda**：
```bash
conda create -n vrp-alns python=3.10
conda activate vrp-alns
```

### 步骤2：安装依赖

```bash
# 安装基础依赖
pip install -r requirements.txt

# 如果PyTorch Geometric安装失败，参考问题2
```

### 步骤3：验证安装

创建测试脚本 `check_env.py`：
```python
import sys
print(f"Python版本: {sys.version}")

try:
    import torch
    print(f"✓ PyTorch: {torch.__version__}")
    print(f"  CUDA可用: {torch.cuda.is_available()}")
except ImportError:
    print("✗ PyTorch未安装")

try:
    import torch_geometric
    print(f"✓ PyTorch Geometric: {torch_geometric.__version__}")
except ImportError:
    print("✗ PyTorch Geometric未安装")

try:
    import numpy
    print(f"✓ NumPy: {numpy.__version__}")
except ImportError:
    print("✗ NumPy未安装")

try:
    import scipy
    print(f"✓ SciPy: {scipy.__version__}")
except ImportError:
    print("✗ SciPy未安装")

try:
    import matplotlib
    print(f"✓ Matplotlib: {matplotlib.__version__}")
except ImportError:
    print("✗ Matplotlib未安装")

print("\n检查项目文件...")
import os
files = ['train_reinforce.py', 'test.py', 'actor.py', 'critic.py', 'vrp_data.py']
for f in files:
    if os.path.exists(f):
        print(f"✓ {f}")
    else:
        print(f"✗ {f} 未找到")
```

运行检查：
```bash
python check_env.py
```

期望输出：
```
Python版本: 3.10.x
✓ PyTorch: 2.x.x
  CUDA可用: True/False
✓ PyTorch Geometric: 2.x.x
✓ NumPy: 1.x.x
✓ SciPy: 1.x.x
✓ Matplotlib: 3.x.x

检查项目文件...
✓ train_reinforce.py
✓ test.py
✓ actor.py
✓ critic.py
✓ vrp_data.py
```

---

## 问题5：Windows路径问题

### 错误信息
```
FileNotFoundError: [Errno 2] No such file or directory: './checkpoints/...'
```

### 解决方案

**确保目录存在**：
```python
import os
os.makedirs('checkpoints', exist_ok=True)
```

**或者在运行前手动创建**：
```bash
# Windows
mkdir checkpoints

# Linux/macOS
mkdir -p checkpoints
```

---

## 问题6：权限错误

### 错误信息（Linux/macOS）
```
PermissionError: [Errno 13] Permission denied: './checkpoints/actor_xxx.pth'
```

### 解决方案

```bash
# 检查目录权限
ls -la checkpoints/

# 修改权限
chmod 755 checkpoints/
chmod 644 checkpoints/*.pth
```

---

## 运行测试

### 快速测试（使用小规模数据）

修改 `train_reinforce.py` 创建测试版本 `train_test.py`：
```python
# 小规模配置
num_customers = 20        # 100 → 20
vehicle_capacity = 15     # 50 → 15
epochs = 5                # 200 → 5
batches = data_generator(num_customers, vehicle_capacity,
                        instances=12, batch_size=2)  # 120 → 12, 6 → 2
budget = 10               # 50 → 10
```

运行：
```bash
python train_test.py
```

预期运行时间：5-10分钟（CPU）

---

## 性能优化建议

### 1. 使用GPU加速
```python
# 确认CUDA可用
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
print(f"Using device: {device}")
```

### 2. 多进程数据加载
```python
from torch_geometric.loader import DataLoader

loader = DataLoader(graphs, batch_size=6, num_workers=4)  # 添加num_workers
```

### 3. 混合精度训练（需要GPU）
```python
from torch.cuda.amp import autocast, GradScaler

scaler = GradScaler()

with autocast():
    # 前向传播
    action, log_prob = actor_model(state, mask)

# 反向传播
scaler.scale(loss).backward()
scaler.step(optimizer)
scaler.update()
```

---

## 调试技巧

### 1. 启用详细日志
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### 2. 检查梯度
```python
# 在训练循环中添加
for name, param in actor_model.named_parameters():
    if param.grad is not None:
        print(f"{name}: grad_norm = {param.grad.norm()}")
```

### 3. 可视化训练过程
```python
# 记录损失
losses = []
for epoch in range(epochs):
    loss = train_one_epoch()
    losses.append(loss)

# 绘制
plt.plot(losses)
plt.xlabel('Epoch')
plt.ylabel('Loss')
plt.savefig('training_loss.png')
```

---

## 常用命令速查

```bash
# 训练（默认配置）
python train_reinforce.py

# 测试（单种子）
python test.py --checkpoint ./checkpoints/actor_c100_v50_final.pth \
               --customers 100 \
               --capacity 50 \
               --budget 2000 \
               --batch_size 20 \
               --seed 123

# 测试（多种子）
python test.py --checkpoint ./checkpoints/actor_c100_v50_final.pth \
               --customers 100 \
               --capacity 50 \
               --budget 2000 \
               --batch_size 20 \
               --multi_seed \
               --num_seeds 5

# 检查GPU使用情况
nvidia-smi

# 查看进程
# Windows
tasklist | findstr python
# Linux
ps aux | grep python
```

---

## 获取帮助

如果遇到其他问题：

1. **检查Python版本**：建议使用 Python 3.8-3.10
2. **查看完整错误栈**：运行时添加 `--verbose` 参数
3. **搜索类似问题**：
   - PyTorch: https://github.com/pytorch/pytorch/issues
   - PyTorch Geometric: https://github.com/pyg-team/pytorch_geometric/issues
4. **查看依赖兼容性**：
   - https://pytorch.org/get-started/locally/
   - https://pytorch-geometric.readthedocs.io/

---

## 附录：推荐的开发环境

### IDE选择
- **VSCode**：轻量级，插件丰富
  - 推荐插件：Python, Pylance, Jupyter
- **PyCharm**：功能强大，调试方便
  - 专业版支持远程开发

### Python版本
- **推荐**：Python 3.10
- **支持**：Python 3.8, 3.9
- **不推荐**：Python 3.11+（部分依赖可能不兼容）

### 硬件要求
- **最低配置**：8GB RAM，CPU训练
- **推荐配置**：16GB RAM，NVIDIA GPU（6GB+ VRAM）
- **理想配置**：32GB RAM，NVIDIA GPU（12GB+ VRAM）

### 训练时间估算
| 配置 | 客户数 | Epochs | CPU时间 | GPU时间 |
|------|--------|--------|---------|---------|
| 小规模 | 20 | 50 | 30分钟 | 5分钟 |
| 中规模 | 50 | 100 | 3小时 | 30分钟 |
| 大规模 | 100 | 200 | 12小时 | 2小时 |

---

最后更新：2024年
